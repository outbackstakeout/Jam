import React from "react";
import "./SidebarChannel.css";

function SidebarChannel({ jam }) {
    return (
        <div className="sidebarChannel">
            <h4>
                <span className="sidebarChannel_hash">#</span>YouTube
            </h4>
        </div>
    );
}

export default SidebarChannel;
